[project]
name = "uw-tube-furnace-pid"
version = "0.1.0"
description = "PID/ramp controller for a Delta DTB tube furnace"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.9",
    "pyserial>=3.5",
    "readchar>=4.2.2",
    "rich>=15.0.0",
]
